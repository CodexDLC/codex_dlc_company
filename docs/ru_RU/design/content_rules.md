# 📜 Правила и архитектура контента (Content Rules & Architecture)

## 1. Общие принципы

Этот документ закрепляет архитектурные решения и правила работы с контентом на платформе CodexDLC. Агент (AI-помощник) и разработчики должны руководствоваться этими правилами при создании, изменении или переводе контента.

## 2. Режимы работы сайта (Site Modes)

В модели `SiteConfig` существует поле `site_mode`, определяющее текущий режим работы лендинга:
- **`'research'`**: Режим исследования. Лендинг ориентирован на сбор email-адресов, проведение опросов. Продажи напрямую не осуществляются. CTA-элементы ведут на формы опросов или подписки.
- **`'sales'`**: Режим продаж. Лендинг ориентирован на прямые продажи услуг. CTA-элементы ведут на страницы тарифов, формы заказа или консультации.

**Правило для Агента/Разработчика:**
- Все кнопки и CTA-элементы, зависящие от режима, должны иметь ДВА варианта текста (для `research` и `sales`).
- Логика переключения текста по режиму реализуется на уровне шаблонов/бэкенда. Агент НЕ меняет логику, только подставляет нужный вариант текста.
- Остальные блоки (Hero, услуги, доверие, Tech Stack) остаются одинаковыми в обоих режимах, если явно не указано иное.

## 3. Контентные константы и важные примечания

- **Правило 48 часов vs Запуск:**
    - **48 часов:** Относится только к созданию *кликабельного прототипа* (без форм и букинга).
    - **7–14 дней:** Относится к *полноценному запуску* сайта после утверждения прототипа.
    - Агент/Разработчик НИКОГДА не путает эти два срока в коммуникациях и контенте.

- **§19 UStG (Малый бизнес):**
    - Под ценами, где это применимо, должна быть сноска о применении §19 UStG (освобождение от НДС для малого бизнеса в Германии).

- **eRecht24 (DSGVO/Impressum):**
    - Все юридические тексты (Impressum, Datenschutzerklärung) должны соответствовать стандартам eRecht24 и DSGVO.
    - В контенте подчеркивается полная защита по DSGVO с первого дня.

## 4. Логика отображения и структуры

- **Шаблон для страниц услуг:**
    - Все страницы услуг (`/services/waas/`, `/services/automation/`, `/services/solutions/`) должны следовать единому шаблону структуры блоков (Hero, Для кого, Что входит/Как работает, Тарифы, Проекты, Финальный CTA).

- **Бенто-логика для проектов (адаптивная сетка):**
    - Используется адаптивная 12-колоночная бенто-сетка.
    - Логика `col-span` классов рассчитывается через `templatetag bento_span(index, total)`:
        - 1 проект → `col-span-12`
        - 2 проекта → `col-span-6`
        - 3 проекта → первый `col-span-6`, остальные `col-span-3`
        - 4 проекта → `col-span-6` каждый (2x2)
        - 5 проектов → первые два `col-span-6`, остальные `col-span-4`
        - 6 проектов → `col-span-4` каждый (3x2 стандарт)

- **Z-блоки для услуг:**
    - На главной странице услуги представлены в виде Z-блоков, каждый из которых ведет на отдельную страницу услуги.

## 5. Интеграция с базой данных (БД)

- **Динамический контент:**
    - Проекты (на страницах услуг и решений) берутся из модели `Project` в БД.
    - Тарифы (на страницах услуг) берутся из соответствующей модели `PricingPlan` или аналогичной.
    - Агент/Разработчик НЕ хардкодит динамический контент.

- **Статический контент:**
    - Блоки "Как это работает" (на странице решений) и "TECH STACK" (в подвале) являются статичными и не берутся из БД. Тексты для них зашиты в шаблоне.

- **Модель `Project`:**
    - Поля для карточек проектов: `name`, `niche`, `city`, `status` (`live` | `taken` | `free`), `preview_url`, `site_url`, `thumbnail`.
    - Для фильтрации проектов по автоматизации используется поле `has_automation = BooleanField`.

## 6. Мультиязычность

- **Поддерживаемые языки:** DE (немецкий), RU (русский), EN (английский), UA (украинский).
- **Технические ключи:** Ключи типа `cta_primary`, `feat_1`, `card_name` и т.д. остаются на английском языке для консистентности с кодом и моделями БД. Переводятся только значения этих ключей.
- **Синхронизация:** При добавлении нового контента или изменении существующего, он должен быть синхронизирован на всех поддерживаемых языках.

## 7. Правила для Агента (AI-помощника)

- **Не менять логику:** Агент не меняет логику работы сайта (например, переключение режимов), только предоставляет контент согласно правилам.
- **Не хардкодить:** Агент не хардкодит данные, которые должны браться из БД.
- **Соблюдать структуру:** Агент следует утвержденной структуре блоков и именованию файлов.
- **Уточнять:** Если информации недостаточно или есть неопределенность, Агент обязан задать уточняющие вопросы.
