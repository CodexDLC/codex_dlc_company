# 🎨 Айдентика и стиль (High-Tech Gadget)

## 1. Система цветов (Dark Mode First)
Дизайн имитирует интерфейс премиального электронного устройства. Глубокие темные фоны с яркими неоновыми акцентами и эффектами стекломорфизма (glassmorphism).

### Базовые поверхности (Glassmorphism & Depth)
* **Deep Space (Main BG):** `#050505` — Основной фон для максимального контраста.
* **Glass Panel:** `rgba(255, 255, 255, 0.05)` с эффектом `backdrop-filter: blur(12px)` — Фон для Bento-боксов и плавающего дока.
* **Border Glow:** `rgba(255, 255, 255, 0.1)` — Тонкие границы, имитирующие светящиеся края стекла.

### Акцентные цвета (Неон)
* **Cyber Blue (Primary Action):** `#00F0FF` — Главные кнопки, активные состояния и основные выделения.
* **Electric Purple (Secondary):** `#8A2BE2` — Иконки, градиенты фона и акценты глубины.
* **Status Green:** `#00FF66` — Индикаторы Uptime (99.9%) и успешные статусы.

### Цвета текста
* **Primary Text:** `#FFFFFF` — Заголовки и основная информация.
* **Secondary Text:** `#A1A1AA` — Описания, подзаголовки и вспомогательный текст.

## 2. Типографика
Техничные, геометрические шрифты, сочетающие инженерную точность и читабельность для бизнеса.

* **Семейства шрифтов:**
    * **Заголовки / UI:** `Space Grotesk` (Геометричный, современный, структурный).
    * **Код / Статистика:** `JetBrains Mono` (Моноширинный для технических данных и бейджей).
    * **Основной текст:** `Inter` (Максимальная читабельность).

* **Шкала (Десктоп):**
    * **H1 (Hero):** 56px / 64px line-height, Bold (700).
    * **H2 (Section):** 40px / 48px line-height, Semi-Bold (600).
    * **Body Large:** 18px / 28px line-height, Regular (400).
    * **Tech Labels:** 12px, Uppercase, Medium (500), JetBrains Mono.

## 3. Отступы и геометрия
* **Базовая сетка:** 8px (xs: 8px, sm: 16px, md: 24px, lg: 32px, xl: 48px).
* **Радиус скругления (Border Radius):**
    * **Bento-карточки:** `24px` (Мягкое ощущение премиального гаджета).
    * **Кнопки/Поля ввода:** `0px` (Строгий терминальный/тактильный стиль).
    * **Плавающий dok:** `100px` (Форма капсулы).

---

## 4. Система тем (Dual Mode)

Дизайн-система поддерживает два визуальных режима, компилируемых как отдельные CSS-бандлы.

### Тёмная тема — "High-Tech Gadget" (по умолчанию)
Основной бренд-опыт. Чёрный фон, неоновые акценты в цвете cyber blue, glassmorphism-панели.

| Токен | Значение | Назначение |
|-------|----------|-----------|
| `--deep-space` | `#050505` | Фон страницы |
| `--glass-bg` | `rgba(255,255,255,0.07)` | Фон карточек/поверхностей |
| `--glass-bg-elevated` | `rgba(255,255,255,0.10)` | Приподнятые поверхности, hover |
| `--glass-border` | `rgba(255,255,255,0.12)` | Границы карточек |
| `--glass-border-hover` | `rgba(0,240,255,0.25)` | Hover-свечение границ |
| `--glow-btn` | двойной box-shadow | Неоновое свечение кнопок |
| `--dock-bg` | `rgba(15,15,15,0.88)` | Фон плавающего дока |
| `--glass-glow-blue` | `rgba(0,240,255,0.08)` | Свечение карточки при hover |
| `--glass-glow-purple` | `rgba(138,43,226,0.07)` | Глубина фона hero-секции |

### Светлая тема — "Premium Studio"
Чистые, воздушные поверхности в Mac-стиле. Та же геометрия, тени заменяют glow-эффекты.

| Токен | Значение | Назначение |
|-------|----------|-----------|
| `--deep-space` | `#F2F2F7` | Фон страницы (Apple light gray) |
| `--glass-bg` | `rgba(255,255,255,0.72)` | Матовые стеклянные карточки |
| `--glass-border` | `rgba(0,0,0,0.08)` | Тонкие границы |
| `--dock-bg` | `rgba(255,255,255,0.85)` | Белый матовый dok |
| `--cyber-blue` | `#00838F` | Адаптирован для читаемости на светлом |
| `--electric-purple` | `#6B21D6` | Адаптирован для читаемости на светлом |
| `--status-green` | `#00964A` | Адаптирован для читаемости на светлом |

### Сборка (Build Pipeline)
```
base.css       → python tools/static/css_compiler.py → app.css       (dark)
base-light.css → python tools/static/css_compiler.py → app-light.css (light)
```

Компилятор читает `compiler_config.json` и рекурсивно раскрывает все `@import`.

### Переключение тем (JS)
Активный CSS-бандл переключается динамически через `<link id="theme-css">`.

- **По умолчанию:** Следует системному `prefers-color-scheme`
- **Ручной выбор:** Сохраняется в `localStorage` с ключом `codex-theme`
- **Без мигания:** Маленький inline-скрипт в `<head>` применяет тему до рендера
- **Кнопка переключения:** `[ ◗ ]` (тёмная) / `[ ☀ ]` (светлая) — в стиле терминала, в шапке
