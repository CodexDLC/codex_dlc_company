# 🧩 UI-компоненты и взаимодействие

## 1. Кнопки (Тактильный терминальный стиль)
Кнопки должны ощущаться как физические аппаратные триггеры или строгие консольные команды. Никаких мягких теней.

* **Основное действие (Cyber Blue):**
    * *По умолчанию:* Фон `transparent`, граница `1px solid #00F0FF`, текст `#00F0FF`, `0px` border-radius.
    * *Наведение:* Фон `#00F0FF`, текст `#050505`. Мгновенный переход (`75ms`).
    * *Активное состояние:* Фон `#00C4D1`, трансформация `scale(0.98)`.
* **Вторичное действие (Surface):**
    * *По умолчанию:* Фон `rgba(255, 255, 255, 0.05)`, граница `1px solid rgba(255, 255, 255, 0.1)`, текст `#FFFFFF`.
    * *Наведение:* Фон `rgba(255, 255, 255, 0.1)`, граница `1px solid #A1A1AA`.

## 2. Формы и поля ввода (Эстетика командной строки)
Поля ввода имитируют командную строку, полностью исключая стандартную стилизацию веб-форм.

* **Поле ввода по умолчанию:**
    * Фон `transparent`, только нижняя граница `1px solid rgba(255, 255, 255, 0.1)`.
    * Текст `#FFFFFF`, шрифт `JetBrains Mono`.
* **Состояние фокуса:**
    * Нижняя граница `1px solid #00F0FF`. Цвет каретки `#00F0FF`.
* **Состояние ошибки:**
    * Нижняя граница `1px solid #EF4444`. Текст валидации шрифтом `12px JetBrains Mono`.

## 3. Bento-карточки (Glassmorphism)
Используются для модулей услуг и ключевых функций.

* **Структура (тёмная тема):**
    * Фон: `linear-gradient(135deg, rgba(255,255,255,0.10), rgba(255,255,255,0.07))`.
    * Backdrop-filter: `blur(12px)` через `var(--glass-blur)`.
    * Граница: `1px solid rgba(255,255,255,0.12)` через `var(--glass-border)`.
    * Радиус скругления: `24px` через `var(--radius-bento)`.
    * Inset-тень: `inset 0 1px 0 rgba(255,255,255,0.06)`.
* **Эффект наведения:** Граница → `var(--glass-border-hover)` = `rgba(0,240,255,0.25)`.

## 4. Статусная точка (Status Dot)
Пульсирующий зелёный индикатор — uptime / онлайн-состояние.
```css
.status-dot { width: 8px; height: 8px; background: var(--status-green); border-radius: 50%; animation: status-pulse 2s ease-in-out infinite; }
@keyframes status-pulse { 0%,100% { box-shadow: 0 0 4px var(--status-green); } 50% { box-shadow: 0 0 12px var(--status-green), 0 0 24px rgba(0,255,102,0.25); } }
```

## 5. Tech Label
Моноширинная метка в верхнем регистре — идентификатор секции.
* Шрифт: `var(--font-mono)` (JetBrains Mono), `11px`, uppercase, `letter-spacing: 2px`.
* Цвет: `var(--cyber-blue)`.
* Display: `block`, `margin-bottom: 16px`.

## 6. Z-Visual Block
Glassmorphism-карточка-заглушка внутри Z-паттерн строк. Содержит абсолютный акцентный оверлей.

```html
<div class="z-visual blue-accent">
    <div class="z-visual-accent"></div>
    <!-- контент на z-index: 1 -->
</div>
```

* `.z-visual` — стеклянная карточка, `min-height: 340px`, flex по центру, моно шрифт.
* `.z-visual-accent` — `position: absolute; inset: 0` — пурпурный или синий gradient glow.
* `.z-visual.blue-accent .z-visual-accent` — переопределяет на `var(--glass-glow-blue)`.

## 7. Z-Link
Моноширинная ссылка-стрелка в нижней части Z-паттерн текстового блока.
```html
<a href="#" class="z-link">ПОДРОБНЕЕ &nbsp;→</a>
```
* Без подчёркивания. `gap` анимируется с `8px` до `16px` при наведении (`transition: gap 0.2s ease`).
* Цвет: `var(--cyber-blue)`, размер: `12px`, uppercase, моно.

## 8. Tech Badge
Моно-пилюля для отображения технологического стека.

```html
<span class="tech-badge">Docker</span>
<span class="tech-badge active">Python</span>
```

* По умолчанию: фон `var(--glass-bg)`, граница `var(--glass-border)`, текст `var(--gray-400)`.
* `.active`: `border-color: var(--cyber-blue)`, `color: var(--cyber-blue)`.

## 9. Контраст-карточки (Contrast Cards)
Используются в Блоке 2 (Проблема vs Решение).

```html
<div class="contrast-card">          <!-- неактивная -->
<div class="contrast-card active">   <!-- активная/решение -->
```

* По умолчанию: glass-фон, стандартная граница. Hover: `border-color: rgba(138,43,226,0.3)`.
* `.active`: `border-color: var(--cyber-blue)`, `box-shadow: 0 0 32px rgba(0,240,255,0.06)`, gradient-фон.
* `.contrast-list li::before` — `—` (неактивная) vs `→` в cyber-blue (активная).

## 10. Marquee
Бесконечный горизонтальный тикер с названиями портфолио.
```html
<div class="marquee">
    <div class="marquee-content">
        <span class="marquee-item">LILY SALON</span>
        <span class="marquee-item">//</span>
        <!-- повторяется дважды для бесшовного цикла -->
    </div>
</div>
```
Анимация: `marquee-scroll 30s linear infinite` — translateX `-50%`.

## 11. Island Footer
Скруглённый glassmorphism-футер с 24px отступом от краёв страницы.
```css
.island-footer { margin: 0 24px 24px; padding: var(--space-2xl); border-radius: 40px; }
```
Нижняя строка (`.island-footer-bottom`): flex space-between, моно 10px, копирайт + строка версии.
