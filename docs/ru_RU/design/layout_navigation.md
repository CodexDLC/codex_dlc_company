# 📐 Макет и навигация

## 1. Сетка (Bento Box)
Макет использует асимметричную модульную сетку (CSS Grid). Каждый блок услуги или функции представлен как отдельный виджет (Bento-box) с радиусом скругления `24px`.

### Контрольные точки (Breakpoints)
* **Мобильные (xs):** `< 640px` — 4 колонки, отступы 16px.
* **Планшеты (sm/md):** 640px - 1024px — 8 колонок, отступы 24px.
* **Десктоп (lg):** 1024px - 1440px — 12 колонок, отступы 32px.
* **Ультраширокие (xl):** `> 1440px` — Контейнер с макс. шириной `1440px`, центрированный.

## 2. Навигация (Плавающий док)
Вместо традиционного верхнего хедера используется «Плавающий док» в нижней части экрана.

* **Расположение:** `fixed`, `bottom: 24px`, `left: 50%`, `transform: translateX(-50%)`.
* **Визуал:** Форма капсулы (радиус 100px), фон Glassmorphism, тонкая полупрозрачная рамка.
* **Элементы:**
    * Домой (Логотип CD).
    * Услуги (Иконка шестеренки).
    * Портфолио (Иконка сетки).
    * Связаться (Кнопка в цвете Cyber Blue).
* **Поведение:** Сжимается или становится прозрачнее при скролле. Подсвечивается при наведении.

## 3. Верхняя панель (Минимализм)
Верхняя область остается чистой для брендинга и утилит. `position: absolute`, полная ширина, `z-index: 10`.
* **Слева:** `Codex` + `DLC` (акцент cyber-blue) — `Space Grotesk`, 24px, bold.
* **Справа (flex-ряд):** Кнопка переключения темы + переключатель языков.

## 4. Z-паттерн макет
Чередующиеся двухколоночные строки для секции «Услуги» (Блок 3). Каждая строка — одна услуга.

### Структура
```
.z-section        — обёртка, padding: 96px 0
.z-row            — CSS Grid 1fr / 1fr, gap: 80px, align-items: center
.z-row.reverse    — direction: rtl (меняет колонки без изменения порядка DOM)
.z-row.reverse>*  — direction: ltr (восстанавливает направление текста/контента)
.z-text-block     — flex column, gap: 20px (метка + h2 + p + список + z-link)
.z-visual         — glassmorphism-карточка, min-height: 340px, overflow: hidden
.z-visual-accent  — абсолютный overlay: пурпурное или синее свечение
```

### Визуальные акценты
* `.z-visual` по умолчанию: пурпурный акцент через `var(--glass-glow-purple)`.
* `.z-visual.blue-accent`: переключается на `var(--glass-glow-blue)`.

### Адаптив (≤768px)
`.z-row` и `.z-row.reverse` схлопываются в одну колонку (`direction: ltr`, `gap: 40px`).

## 5. Переключатель темы
Кнопка в терминальном стиле в верхней панели переключает CSS-бандлы тёмной и светлой тем.

### HTML
```html
<button class="theme-toggle" onclick="toggleTheme()" id="theme-btn">[ L ]</button>
```
Метка `[ L ]` — тёмный режим (нажать для светлого), `[ D ]` — светлый режим.

### JS-логика
```javascript
var DARK_CSS  = 'src/backend_django/static/css/app.css';
var LIGHT_CSS = 'src/backend_django/static/css/app-light.css';

function applyTheme(theme) {
    document.getElementById('theme-css').href = theme === 'light' ? LIGHT_CSS : DARK_CSS;
    localStorage.setItem('codex-theme', theme);
}
function toggleTheme() { applyTheme(getTheme() === 'dark' ? 'light' : 'dark'); }
```

### Защита от мигания (Anti-Flash)
Инлайн `<script>` в `<head>` выполняется до рендера стилей. Читает `localStorage`, при необходимости устанавливает `data-theme="light"` на элементе `<html>` — правильный CSS-бандл применяется до первой отрисовки.
