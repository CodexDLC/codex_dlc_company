# 🏛️ Архитектура системы и оркестрация (WaaS платформа)

## 1. Обзор
Платформа **CodexDLC** реализует бизнес-модель WaaS (Website as a Service). Архитектура построена по принципу **Isolated Multi-Tenancy** (Изолированная мультитенантность). Вместо единой монолитной базы данных для всех клиентов, каждый бизнес получает собственную изолированную среду (Docker-контейнер, БД, Redis), которая разворачивается из стандартизированного шаблона `project-template`.

Это обеспечивает нулевое взаимное влияние клиентов друг на друга, максимальную безопасность данных и легкость в обновлении или переносе инфраструктуры конкретного заказчика.

## 2. Основные узлы (Core Nodes)

### 2.1. Gateway Node (codex-dlc.com)
Главная витрина и распределительный шлюз платформы.
* **Стек:** Django, HTMX, PostgreSQL.
* **Функции:**
    * Презентация услуг и "шлюзы" для различных ниш (Z-Pattern Landing).
    * Лидогенерация и первичная квалификация через интерактивные FSM-формы (HTMX).
    * Панель администратора (CRM-модуль) для управления подписками и статусами развертывания клиентских проектов.

### 2.2. Orchestrator & Deployment Pipeline
Автоматизированная фабрика сборки.
* **Стек:** Bash, Python (CLI `init_project`), GitHub Actions, Docker Compose.
* **Функции:**
    * При поступлении новой заявки запускается скрипт `init_project`, который клонирует нужную ветку `project-template` (Django или FastAPI).
    * Автоматическая подмена `.env` переменных (имена БД, токены ботов, порты).
    * Генерация конфигураций Nginx (через `site.conf.template`) для маршрутизации домена клиента на выделенный порт.
    * Развертывание нового изолированного стека (Backend, Redis, Worker, Bot) на сервере.

### 2.3. Tenant Instances (Клиентские узлы)
Готовые работающие продукты для локального бизнеса (например, салон LILY или монтажники OCS).
* **Стек:** Django/FastAPI, Redis, Celery/Arq (Workers), PostgreSQL.
* **Функции:**
    * Автономная работа бизнес-логики клиента (запись на прием, корзина, опросники).
    * Собственная админка и независимая база данных.
    * Связь с центральной системой (CodexDLC) через Webhooks или Redis Pub/Sub для отправки телеметрии (Uptime, ошибки).

### 2.4. Central Telegram Hub (Нервная система)
Единый командный центр для связи и управления.
* **Стек:** Aiogram 3, Redis, Webhooks.
* **Функции:**
    * Агрегация всех заявок с `Gateway Node`.
    * FSM-сценарии для онбординга новых клиентов (опросники в боте).
    * Уведомления об алертах с `Tenant Instances` (например, падение клиентского контейнера).

## 3. Поток данных (Процесс создания подписки)

1.  **Lead Capture:** Пользователь оставляет заявку на `codex-dlc.com` (Gateway Node).
2.  **Notification:** Данные пробрасываются в Redis Queue -> Telegram Hub уведомляет Архитектора.
3.  **Provisioning:** Через CLI-инструмент (Orchestrator) запускается команда:
    `python -m tools.init_project --type django --client "new_salon" --domain "new-salon.de"`
4.  **Deployment:** CI/CD пайплайн собирает Docker-образы, обновляет Nginx reverse-proxy и выдает SSL сертификат (Certbot).
5.  **Delivery:** Система отправляет клиенту доступы от его независимой инстанции.
